import { labels } from "@i18n/labels";

const textsConfig = {
  noResultsMessage: {
    title: labels.login.noResultsMessage.title,
    suggestion: labels.login.noResultsMessage.suggestion,
  },

  clientsTitle: labels.login.clients.title,
  clientsSubtitle: labels.login.clients.subtitle,

  searchPlaceholder: labels.login.search.placeholder,

  continueButton: labels.login.actions.continue,
};

export { textsConfig };
